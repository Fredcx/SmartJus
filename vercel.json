{
    "version": 2,
    "functions": {
        "api/index.ts": {
            "includeFiles": "node_modules/{puppeteer-extra-plugin-*,fs-extra**,universalify**,graceful-fs**,jsonfile**,deepmerge**,rimraf**,mkdirp**,p-queue**}/**"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}